import { CHEFS } from '../shared/chefs';

export const Chefs = (state = CHEFS, action) => {
    switch (action.type) {
        // case value:

        //     break;

        default:
            return state;
    }
};